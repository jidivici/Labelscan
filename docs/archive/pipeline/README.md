# Archived pipeline references

`shared-state.schema.json` describes an earlier shared-state design. The running pipeline
no longer treats this standalone file as a contract.

Use the [pipeline architecture](../../pipeline/PIPELINE-ARCHITECTURE.md), the current
backend models, and automated tests when changing processing state or worker behavior.
